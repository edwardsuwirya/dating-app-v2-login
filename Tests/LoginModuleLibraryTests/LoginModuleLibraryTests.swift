import XCTest
@testable import LoginModuleLibrary

final class LoginModuleLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LoginModuleLibrary().text, "Hello, World!")
    }
}
